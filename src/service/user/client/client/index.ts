import { Client } from "../../../base/client";

export class ClientClient extends Client<
  unknown,
  unknown,
  unknown,
  unknown,
  unknown,
  unknown,
  unknown,
  unknown,
  unknown
> {
  constructor() {
    super("client");
  }
}
